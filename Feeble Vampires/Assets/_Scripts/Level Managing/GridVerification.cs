using UnityEngine;

public class GridVerification : MonoBehaviour
{
    /*/// <summary>
    /// Checks if a passed in grid is viable
    /// </summary>
    /// <returns></returns>
    public bool DVerify(out List<Vector2Int> sigilLocations, out List<Vector2Int> exitLocations, int width, int height)
    {
        int gridSize = height * width * 8;
        sigilLocations = new List<Vector2Int>();
        exitLocations = new List<Vector2Int>();

        List<DTile> tiles = new List<DTile>();
        // initialize all tiles
        for (int row = 0; row < height * 8; row++)
        {
            for (int col = 0; col < width * 8; col++)
            {
                DTile temp = new DTile(int.MaxValue, null, new Vector2Int(col, row));
            }
        }


    }*/
}
